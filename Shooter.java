
interface Shooter {
    // an abstract method (notice: don't need 'abstract prefix')
    /* 'abstract' method -- when called ('SoccerPlayer' class) -> will need to be overridden (Polymorphic)  */
    public String takeShot(String shot);
}